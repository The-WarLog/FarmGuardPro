# Frontend env (Vite)
# Set this to your deployed backend URL (no trailing slash)
VITE_API_BASE_URL=https://your-backend-domain.com

# If detection API is separate, set it here
VITE_DETECTION_API_URL=https://your-detection-domain.com/predict
